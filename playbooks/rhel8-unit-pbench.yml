---
- hosts: node1,node2,node3
  tasks:

    - name: "rhel8-unit-pbench : yum install buildah,podman"
      yum: name=buildah,podman state=installed
      register: result
      retries: 10
      until: result is succeeded
      delay: 5
